name: CI

on:
  workflow_dispatch:

jobs:
  build-and-test:
    name: Build & Test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build (SPM)
        run: cd MacSweep && swift build
      - name: Test (SPM)
        run: cd MacSweep && swift test
